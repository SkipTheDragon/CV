{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <body>
        <div class="container">
            <div class="row">
                <div class="col">
                    <ul class="list-group list-group-horizontal text-monospace tab">
                        <li class="list-group-item  active tab-button"><a href="{{ path('admin') }}"><span>Dashboard</span></a></li>
                        <li class="list-group-item"><a href="{{ path('admin_manage', {'slug': 'components'}) }}"><span>Components</span></a></li>
                        <li class="list-group-item"><a href="{{ path('admin_manage', {'slug': 'types'}) }}"><span>Types</span></a></li>
                    </ul>
                </div>
                <div class="col text-right align-self-center"><button class="btn btn-link downloadbtn" type="button"><a href="{{ path('home') }}">Back to profile</a></button>
            </div>
        </div>

    <div>
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    {{ include('admin/components/profile.html.twig') }}
                </div>
                <div class="col-md-6">
                </div>
            </div>
        </div>
    </div>
    </body>
{% endblock %}
